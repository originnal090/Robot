# Optional environment for hcirobot@.service.
PYTHONUNBUFFERED=1
# Keep deployment offline and deterministic: build /opt/hcirobot/.venv before
# enabling the service. The unit does not invoke uv or pip.
